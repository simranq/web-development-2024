const hobbies = [ 'Sports', 'Cooking']; // a pointer to the array is stored
hobbies.push('Reading');    // the value itself is stored
console.log(hobbies);
// arrs are spl objs. objs are stored in  mmy like strings, boolean, numbers, undefined-> primitive values(simple)
//reference vals -> arrays, objs-> stored 